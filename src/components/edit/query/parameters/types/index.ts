export type ParametersType = {
	type: string;
	id: string;
	label: string;
	options: string[];
	defaultValue: string;
	prefix: string; // should be void  - use key
	postfix: string; // void??
	info: string;
	key: string;
};
