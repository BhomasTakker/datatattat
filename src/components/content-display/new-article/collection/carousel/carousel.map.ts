import { getDisplayCarouselRenderObject } from "./source-objects/display-carousel";

export const carouselMap = new Map([
	// We can add in a stack-carousel
	["carousel-display", getDisplayCarouselRenderObject],
]);
