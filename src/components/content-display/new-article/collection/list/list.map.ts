import { getStandardListRenderObject } from "./source-objects/standard";

export const listMap = new Map([
	// We can add in a stack-carousel
	["list-standrd", getStandardListRenderObject],
]);

// export const getListRenderObject = (id: string) => {
// 	return stackMap.get(id);
// };
