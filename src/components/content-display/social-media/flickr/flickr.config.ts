const scriptSrc = "";

export const BASE_URL = `https://www.flickr.com/services/oembed/`;
export const ROOT = "https://flickr.com/";

// find one
export const fallback = "";

export enum FlickrEmbedType {
	user = "user",
	page = "page",
}

export const flickrConfig = {
	scriptSrc,
};
