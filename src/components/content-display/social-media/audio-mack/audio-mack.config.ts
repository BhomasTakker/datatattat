const scriptSrc = "";

export const BASE_URL = `https://audiomack.com/oembed`;
export const ROOT = "https://audiomack.com/";

// find one
export const fallback = "";

export enum AudioMackEmbedType {
	song = "song",
	album = "album",
	playlist = "playlist",
}

export const audioMackConfig = {
	scriptSrc,
};
