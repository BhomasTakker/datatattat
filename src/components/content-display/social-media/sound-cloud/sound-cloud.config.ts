const scriptSrc = "";

export const BASE_URL = `https://soundcloud.com/oembed`;
export const ROOT = "https://soundcloud.com/";

export const fallback = "";

export enum SoundCloudEmbedType {
	route = "route",
}

export const soundCloudConfig = {
	scriptSrc,
};
