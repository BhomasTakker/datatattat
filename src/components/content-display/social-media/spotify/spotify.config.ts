const scriptSrc = "";

export const BASE_URL = `https://open.spotify.com/oembed`;
export const ROOT = "https://open.spotify.com/p/";

export const fallback = "";

export enum SpotifyEmbedType {
	route = "route",
}

export const spotifyConfig = {
	scriptSrc,
};
