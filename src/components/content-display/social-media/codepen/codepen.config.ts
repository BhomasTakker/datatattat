const scriptSrc = "";

export const BASE_URL = `https://codepen.io/api/oembed`;
export const ROOT = "https://codepen.io/";

export const fallback = "";

export enum CodePenEmbedType {
	route = "route",
}

export const codePenConfig = {
	scriptSrc,
};
